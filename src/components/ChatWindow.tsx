import * as React from 'react';

export class ChatWindow extends React.Component {
    constructor(props: any) {
        super(props);
    }

    render(): JSX.Element {
        return (
            <div>messages</div>
        );
    }
}
