import * as React from 'react';

export class Channel extends React.Component {
    constructor(props: any) {
        super(props);
    }

    render(): JSX.Element {
        return (
            <div>channel</div>
        );
    }
}
