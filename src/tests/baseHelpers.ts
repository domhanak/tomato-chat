export const dispatch = jest.fn((action) => action);

export const authTokenHelper = 'Bearer jwtSecret';
// todo negative result tests

export const resolvedPromise = true;
export const rejectedPromise = false;
